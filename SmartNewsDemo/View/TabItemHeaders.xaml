<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behavior="clr-namespace:SmartNewsDemo.Common.Behavior"
             mc:Ignorable="d"
             x:Class="SmartNewsDemo.View.TabItemHeaders">
    <ContentPage.Content>
        <StackLayout>
            <Frame x:Name="frm"
                   Grid.Row="0"
                   CornerRadius="5"
                   Margin="0,10,0,5"                  
                   BackgroundColor="{Binding Source={x:Reference lbl}, Path=BackgroundColor}">
                <Label x:Name="lbl" 
                   Text="{Binding btnTitle}" 
                   Margin="-10"
                   BackgroundColor="{Binding btnColor}"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center" >
                </Label>
            </Frame>
            <StackLayout x:Name="BottomColor" HeightRequest="3" BackgroundColor="{Binding Source={x:Reference lbl}, Path=BackgroundColor}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>